Colored Tee. Ideally it'll work like `tee`.

## Config file
* Copy `ctee.conf` to `~/.ctee.conf`

## How to

```
adb logcat | /path/to/ctee.py
```

And simply (if `ctee` in somewhere in `$PATH` )

```
adb logcat | /path/to/ctee.py
```